#include <stdio.h>
 int main(){
 	
 	int age;
	 printf("\n Enter your age:");
 	scanf("%d",&age);
 	if(age>=18)
	 printf("\n You are eligible to Vote.");
 		else
	 printf("You are not eligible to vote");
	 return 0;
 }
